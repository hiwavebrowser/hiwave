# Outside-eye: hiwave-macos #54 / #55 / #56 stack (PAINT-0 → DIG-buttons → metrics land)

**Author:** Prometheus · **Date:** 2026-07-17 (grind tick, headless)  
**Lane:** design / outside-eye only — **no merge, no force-push, no spend**  
**Base:** `origin/master@5161571` (#53 grid-span-gap merged)  
**Pins consumed:**  
- `2026-07-16-paint0-glyph-seat-IMPLEMENT.md`  
- `2026-07-16-dig-buttons-stack-IMPLEMENT.md`  
- `2026-07-16-text-paint-FIDELITY-RESIDUAL.md`  
- Atlas RESULT `2026-07-17-paint0-RESULT-seating-exonerated.md`

| PR | Branch | Tip | Scope |
|----|--------|-----|-------|
| **#54** | `atlas/paint0-seat-probe` | `dac2623` | probe/instrument only (+320/−1, 7 files) |
| **#55** | `atlas/dig-buttons-stack` | `7c5d507` | UA `InlineBlock` + unit (+91/−0, 1 file) |
| **#56** | `atlas/metrics-normal-lineheight` | `5f561d0` | metrics model on top of #55 (+106/−20 net on 4 files; **includes** #55 commit) |

Stack relation: `#54 ∥ #55`; `#56 = #55 + metrics commit`. Merge order if shipping as three PRs: **#54 anytime → #55 → #56**. Or fold #55 into #56 land only after #55 gate is honest.

---

## 0. Verdicts (one screen)

| PR | Design | CI / merge gate | Action |
|----|--------|-----------------|--------|
| **#54** | **APPROVE** | pr-aggregate **GREEN**; swarms green | **Merge when convenient** (probe-only; zero behavior) |
| **#55** | **APPROVE design** (pin match) | ~~pr-aggregate **RED** — real, not PATH-BUG~~ → **GREEN post-dig** | ~~**HOLD merge** until form-controls ≤ **8.0**~~ → **APPROVE merge** (see 2026-07-20 outside-eye) |
| **#56** | **APPROVE design** (PAINT-0 land gates met) | ~~pr-aggregate **RED** (inherits #55)~~ → **GREEN** (base = #55 dig tip) | **APPROVE merge after #55** |

**SUPERSEDE 2026-07-20:** Atlas dig RESULT (`2026-07-18-form-controls-t8-DIG-RESULT.md`) + tip `251b105` form-controls **6.42 ≤ 8.0**. Full outside-eye: `2026-07-20-form-controls-t8-DIG-OUTSIDE-EYE.md`. HOLD **lifted**.

Prometheus does **not** merge. Atlas owns merge authority.

---

## 1. #54 PAINT-0 probe — APPROVE

### Pin checklist (`paint0-glyph-seat-IMPLEMENT` §6)

| Check | Result |
|-------|--------|
| Cites night-16 A/B (gallery 6.80 / css-selectors 15.14) | **PASS** |
| Does not treat L684 as primary | **PASS** — untouched |
| P0a–c logs/tables | **PASS** — dense-text fixture + paint0_*.py + RESULT one-pager |
| Land included | **None** — snap trial measured + reverted (css 15.14→15.16) |
| DIG-buttons not smuggled | **PASS** — separate PR |
| Base includes #53 | **PASS** `@5161571` |
| KF/threshold games | **None** |

### CI

All swarms + **pr-aggregate PASS**. Probe is env-gated (`RUSTKIT_PAINT_PROBE=1`); off-by-default path is zero-cost.

### Design takeaway (banked by Atlas RESULT; Prometheus confirms attribution)

- Atlas hash A/B **80/80 identical** → bitmaps exonerated  
- Integral baseline snap **does not** recover the +5.11pp → H1 seating **FALSIFIED**  
- Residual renamed: **integer layout drift** (S6 stack dominant; S4/S1/S3 small)  
- **No AA/gamma epic** before metrics land  
- Model land order inverted vs prior parallel framing: **DIG-buttons first**, then metrics

---

## 2. #55 DIG-buttons-stack — APPROVE design / HOLD merge

### Pin checklist (`dig-buttons-stack-IMPLEMENT` §3 + §5)

| Check | Result |
|-------|--------|
| Minimal UA patch only (`engine/lib.rs` form arm) | **PASS** — one line `display = InlineBlock` + comment |
| Not folded into metrics/paint | **PASS** as standalone PR (though #56 stacks on it) |
| Author cascade can still override | **PASS** — same UA-then-author path as other tags |
| Unit drives real engine | **PASS** — `test_button_ua_display_inline_block_one_line` (3 buttons, shared y, advancing x) |
| DIG-2 height path left alone | **PASS** — no `single_line_box` touch |
| No KF/threshold changes | **PASS** |

### Probe gates (PR body; design-trust, not re-run this seat)

| Gate | Claimed |
|------|---------|
| P2 same-y / x increasing | y=1101 all three; x 35 / 194.8 / 370.7 |
| P3 `.buttons` h ≤42 | **33.4** (was 124.6; Chrome 39) |
| P5 campaign | 24/26; no board flip of about/gallery |
| css-selectors | 10.03 → **14.36** still under campaign t15 |

Honest score move 10.03→14.36 matches the RESULT narrative: removing S6 cancel hides residual section errors. Path back under ~10 is metrics (#56).

### **Blocking CI finding (Prometheus re-sim of `parity_gate.py`)**

| Surface | #54 (probe) | #55 (buttons) | #56 (metrics stack) |
|---------|------------:|--------------:|--------------------:|
| `form-controls` primary VP **800×1200** | **7.978%** | **10.090%** | **10.090%** |
| Category thr (form_controls) | 12 | 12 | 12 |
| **Scope cap (micro → t8)** | **8.0** | **8.0** | **8.0** |
| Gate limit = min(thr, scope, max25) | 8.0 | 8.0 | 8.0 |
| Gate | **PASS** (7.978 &lt; 8) | **FAIL** (10.090 &gt; 8) | **FAIL** (same) |

Simulated failure (matches CI log `form-controls: diff`):

```text
{'case_id': 'form-controls', 'reason': 'diff',
 'diff': 10.090104166666666, 'max_diff': 8.0, 'known_fail': False}
```

**Not PATH-BUG.** Primary-viewport filter works; empty-report tripwire not hit. Exploit-phase 100% rows at non-registry VPs are correctly excluded. The micro scope cap (T6 / CI-1) is the tripwire Atlas campaign board (t12/t15) does not see.

### Why design still APPROVE

Chrome UA is `inline-block` for form controls; RK Block→stack was the named S6 bug. Diff is the minimal pin. The form-controls micro case **regressed past t8** under that correct fix — either:

1. **Honest residual:** other form-controls page structure still wrong after InlineBlock (spacing/margin/replaced sizing) → dig to ≤8 before merge; or  
2. **Ceiling honesty:** if 10.09 is the new truthful band and Pete accepts known_fail + `kf_ceiling≈11.1` (frozen +1 margin style), bank that **explicitly** — do not silent-waive.

**Do not** raise scope cap or category thr without Pete. **Do not** merge on swarm-green alone while aggregate names form-controls.

### Atlas options (ordered)

1. **Preferred:** probe form-controls primary (800×1200) under #55; find why +2.1pp vs #54; land a tiny follow-up if named; re-run aggregate ≤8.  
2. **If irreducible this night:** open a one-line registry known_fail + kf_ceiling for form-controls with Pete ack; separate from #55 body if policy requires.  
3. **Do not:** lower campaign thresholds; fold metrics into #55 to “hide” the red; claim swarm-only merge.

**2026-07-17 later tick — DIG pin banked:** `2026-07-17-form-controls-t8-DIG-IMPLEMENT.md`  
Pre-probe smoking gun: Chrome bare controls h≈**19** (pad 0, border 2px) vs RK bare blob ~**28/32** with UA form arm setting display+font only (no border). InlineBlock is correct; dig is bare height under `single_line_box`, not a revert.

---

## 3. #56 metrics-normal line-height — APPROVE design / HOLD merge

### Land-gate checklist (PAINT-0 pin §4.4 + PR body)

| Gate | Bar | Claimed / CI signal |
|------|-----|---------------------|
| Campaign | ≥24/26 | **25/26** avg ~7.1 (about remains) |
| css-selectors | ≤15 | **12.68** (PR body; shard 800×1200) |
| image-gallery | dual-path ~6.8 t10 | **6.80 PASS** (shard 1280×800) |
| Holdout | 6/6 | claimed |
| Units | engine-driven | css 16/16, layout 246/246, engine 21/21 claimed |
| KF lowers | none | **none in diff** |
| PAINT-0 companion | seating probe done | **#54 + RESULT** — paint exonerated |
| DIG-buttons order | before/with model | **parent commit `7c5d507`** |

### Diff shape (on top of #55)

- `LineHeight::to_px_with_normal` + `NORMAL_LINE_HEIGHT_FALLBACK_RATIO`  
- `normal_line_height` = `round(asc)+round(desc)+leading` (Blink integer mechanism; raw-float 24→23 measured night-13)  
- `resolve_line_height` wired through layout/flex/grid + display-list half-leading path  
- thread-local memo on (family, size bits, weight, style)  
- **No** L684 / paint path rewrite; **no** scorer games

### Residual call-site honesty

Bare `line_height.to_px` remains as **fallback-only** (`to_px` → `to_px_with_normal(..., fs*1.2)`). Production layout paths reviewed on tip go through `resolve_line_height`. Form *1.2 cleanup still **out of scope** (prior residual ledger).

### Hold reason

Aggregate red is **100% inherited from #55 form-controls t8 breach**. Metrics commit does not fix form-controls (same 10.090%). Merging #56 alone without green #55 would land both UA display change and metrics model under a red merge gate.

Once form-controls ≤8 (or known_fail banked):

- **APPROVE merge #56** on design + claimed board receipts  
- Ledger remains: about 16.17; S4 Arial 13.333 miss; optional image-gallery kf ratchet 22.4→14 later

---

## 4. Seat routing

| Seat | Action |
|------|--------|
| **Atlas** | Merge **#54** now if idle. **HOLD #55/#56** until form-controls primary ≤8.0 or Pete known_fail. Prefer fix-then-merge over waive. After green: merge #55 then #56 (or #56 if it still contains #55 and #55 is not merged separately — avoid double-land). |
| **Prometheus** | Outside-eye done this tick. Re-review only if form-controls fix PR is non-trivial or known_fail text drifts. |
| **Athena** | No Windows port until macOS stack green on master. |
| **Pete** | Confirm only if Atlas proposes known_fail / thr move / scope-cap change for form-controls. |

---

## 5. Non-actions (this seat)

- Did **not** merge any of #54/#55/#56  
- Did **not** force-push, amend, or touch master  
- Did **not** edit engine sources  
- Did **not** call `null attend`  
- Did **not** raise KF / thresholds  

---

## 6. Learn (one screen)

PAINT-0 closed the paint epic as a **blocker**: seating and bitmaps are innocent; metrics is landable once layout honesty (DIG-buttons) is in. DIG-buttons is the correct minimal UA fix, but **CI micro scope cap t8** is tighter than the campaign form thr t12 — a +2.1pp form-controls move that still “passes the board” **fails the merge gate**. Swarm-green is necessary and insufficient; re-sim `parity_gate.py --per-case-thresholds --primary-viewport-only` against the aggregate artifact before claiming merge-ready.

— Prometheus / design only / 2026-07-17 grind tick
